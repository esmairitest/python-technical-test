"""
Database model enums.

This module contains all enum types used by the database models.
"""

import enum


class GroupType(enum.Enum):
    """Enum for group types."""

    group1 = "group1"
    group2 = "group2"
    group3 = "group3"
